# Binary
kiwifs
*.test

# Local toolchain / downloaded release binary (not part of source)
.toolchain/
.run-bin/

# Design docs (internal, not part of OSS repo)
kiwifs.md
tasks.md
todo.md
todolist.md

# Environment & secrets
.env
.env.*
*.pem
*.key
secrets/

# Claude Code / Cursor local settings
.claude/

# IDE / editor
.vscode/
.idea/
*.tsbuildinfo

# Runtime data (created by `kiwifs init`)
knowledge/

# Runtime SQLite artifacts (regenerated by kiwifs)
testdata/.kiwi/state/*.db-shm
testdata/.kiwi/state/*.db-wal
testdata/.kiwi/state/sharelinks.json
