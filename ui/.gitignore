node_modules
*.local
.env
.DS_Store
storybook-static
demo-static

# ui/dist contents are regenerated by `npm run build`. We commit a single
# `.gitkeep` so `go build` never fails on a fresh clone where the UI hasn't
# been built yet — the server detects the missing index.html and serves a
# helpful "UI not built" page instead.
dist/*
!dist/.gitkeep
